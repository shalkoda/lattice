{
  "name": "lattice",
  "version": "0.1.0",
  "private": true,
  "description": "Conversational intelligence system that turns engineering discussions into architecture",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "backend": "pnpm --filter backend dev",
    "web": "pnpm --filter web dev",
    "db:setup": "docker-compose up -d",
    "db:down": "docker-compose down"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
